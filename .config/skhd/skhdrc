# Copyright 2023 Nicolas Paul. All rights reserved.
# Use of this source code is governed by a BSD-style
# license that can be found in the LICENSE file.

# Configuration for the skhd hotkey daemon.
# See https://github.com/koekeishiya/skhd
#
# Tips:
#
#   * Command        ⌘             -> `cmd`
#   * Option (Alt)   ⌥             -> `alt`
#   * Shift          ⇧             -> `shift`
#   * Control (Ctrl) ⌃             -> `ctrl`
#   * Hyper          ⌘ + ⇧ + ⌥ + ⌃ -> `hyper`
#
# Also, do not use Option or Shift + Option alone as they provide additional
# letters from non-English alphabets (very useful), e.g. Option + o = ø.

# debug
# alt + cmd - a : open -a Dictionary

# yabai: a powerful tiling window manager
# ctrl       -> everything related to spaces
# cmd        -> everything related to windows
# ctrl + alt -> everything related to displays

# Change the focused window within the current space.
cmd - h : yabai -m window --focus west
cmd - j : yabai -m window --focus south
cmd - k : yabai -m window --focus north
cmd - l : yabai -m window --focus east

# Swap windows between each other inside the current space.
cmd - left  : yabai -m window --swap west
cmd - down  : yabai -m window --swap south
cmd - up    : yabai -m window --swap north
cmd - right : yabai -m window --swap east

# Swap windows between each other inside the current space and create a split.
shift + cmd - left  : yabai -m window --warp west
shift + cmd - down  : yabai -m window --warp south
shift + cmd - up    : yabai -m window --warp north
shift + cmd - right : yabai -m window --warp east

# Change the focused space.
ctrl - 1 : yabai -m space --focus 1
ctrl - 2 : yabai -m space --focus 2
ctrl - 3 : yabai -m space --focus 3
ctrl - 4 : yabai -m space --focus 4
ctrl - 5 : yabai -m space --focus 5
ctrl - 6 : yabai -m space --focus 6
ctrl - 7 : yabai -m space --focus 7
ctrl - 8 : yabai -m space --focus 8
ctrl - 9 : yabai -m space --focus 9
ctrl - 0 : yabai -m space --focus 10

# Move between spaces, and create a circle
ctrl - left  : yabai -m space --focus prev || yabai -m space --focus last
ctrl - right : yabai -m space --focus next || yabai -m space --focus first

# Control spaces.
ctrl - up   : yabai -m space --create
ctrl - down : yabai -m space --destroy

# Move window to a space.
ctrl + cmd - 1 : yabai -m window --space 1
ctrl + cmd - 2 : yabai -m window --space 2
ctrl + cmd - 3 : yabai -m window --space 3
ctrl + cmd - 4 : yabai -m window --space 4
ctrl + cmd - 5 : yabai -m window --space 5
ctrl + cmd - 6 : yabai -m window --space 6
ctrl + cmd - 7 : yabai -m window --space 7
ctrl + cmd - 8 : yabai -m window --space 8
ctrl + cmd - 9 : yabai -m window --space 9
ctrl + cmd - 0 : yabai -m window --space 10

ctrl + cmd - left: yabai -m window --space prev
ctrl + cmd - right : yabai -m window --space next

# Move window to a display
ctrl + alt + cmd - 1 : yabai -m window --space 1
ctrl + alt + cmd - 2 : yabai -m window --space 2
ctrl + alt + cmd - 3 : yabai -m window --space 3
ctrl + alt + cmd - 4 : yabai -m window --space 4
ctrl + alt + cmd - 5 : yabai -m window --space 5
ctrl + alt + cmd - 6 : yabai -m window --space 6
ctrl + alt + cmd - 7 : yabai -m window --space 7
ctrl + alt + cmd - 8 : yabai -m window --space 8
ctrl + alt + cmd - 9 : yabai -m window --space 9
ctrl + alt + cmd - 0 : yabai -m window --space 10

ctrl + alt + cmd - left  : yabai -m window --space prev
ctrl + alt + cmd - right : yabai -m window --space next

# vim:ft=conf
